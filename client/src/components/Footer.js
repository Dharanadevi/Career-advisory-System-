import React from 'react';

const Footer = () => {
  return (
    <footer className="bg-light py-4 mt-auto border-top">
      <div className="container text-center">
        <p className="text-muted mb-0">© 2026 Career Portal. All Rights Reserved.</p>
      </div>
    </footer>
  );
};

export default Footer;

// Note: This Footer component is a simple, reusable component that can be included in the main App.js file to appear on all pages. It uses Bootstrap classes for styling and ensures that it stays at the bottom of the page with `mt-auto`.

